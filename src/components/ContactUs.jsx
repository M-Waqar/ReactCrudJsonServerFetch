import React from "react";

const ContactUs = () => {
  return (
    <div>
      <h1>CONTACT US</h1>
      <p>
        You can now start making GET, POST, PUT, PATCH or DELETE calls to your
        mock server and build the necessary components all the way such that the
        only thing left to do once backend infrastructure is up and running,
        would be to replace the localhost url with the production url.
      </p>
      <p>
        You can now start making GET, POST, PUT, PATCH or DELETE calls to your
        mock server and build the necessary components all the way such that the
        only thing left to do once backend infrastructure is up and running,
        would be to replace the localhost url with the production url.
      </p>
      <p>
        You can now start making GET, POST, PUT, PATCH or DELETE calls to your
        mock server and build the necessary components all the way such that the
        only thing left to do once backend infrastructure is up and running,
        would be to replace the localhost url with the production url.
      </p>
      <p>
        You can now start making GET, POST, PUT, PATCH or DELETE calls to your
        mock server and build the necessary components all the way such that the
        only thing left to do once backend infrastructure is up and running,
        would be to replace the localhost url with the production url.
      </p>
      <div>
        <form>
          <div className="form-group">
            <label>Email address</label>
            <input
              type="text"
              className="form-control"
              placeholder="Enter Email"
            />
          </div>
          <div className="form-group">
            <label>Message</label>
            <input
              type="text"
              className="form-control"
              placeholder="Enter Message"
            />
          </div>
          <button type="submit" className="btn btn-primary">
            Submit
          </button>
        </form>
      </div>
    </div>
  );
};

export default ContactUs;
